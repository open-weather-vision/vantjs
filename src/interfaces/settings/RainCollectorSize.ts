/**
 * The rain collector's size. Is used to convert the (internally used) number of rain clicks to your desired rain unit (see {@link UnitSettings}).
 */
export type RainCollectorSize = "0.01in" | "0.2mm" | "0.1mm";
